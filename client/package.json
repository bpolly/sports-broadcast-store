{
  "name": "sports-broadcasts-store",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:3000/",
  "dependencies": {
    "axios": "^0.16.2",
    "moment-timezone": "^0.5.13",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "1.0.14"
  },
  "scripts": {
    "start": "PORT=3005 react-scripts start",
    "build": "react-scripts build && rm -rf ../public/static && mv build/* ../public",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
